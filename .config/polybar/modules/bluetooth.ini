[module/bluetooth]
type = custom/script

exec = $HOME/.config/polybar/scripts/bluetooth

interval = 1

click-left = $HOME/.config/polybar/scripts/toogle_bluetooth

format-foreground = ${colors.foreground}
format-background = ${colors.background}
